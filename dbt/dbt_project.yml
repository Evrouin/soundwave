name: 'soundwave'
version: '1.0.0'
profile: 'soundwave'

model-paths: ["models"]
test-paths: ["tests"]

models:
  soundwave:
    staging:
      +materialized: view
    intermediate:
      +materialized: view
    marts:
      +materialized: table
